# Server Configuration
PORT=3000

# Target URL (where requests will be forwarded)
TARGET_URL=https://api.example.com

# Authentication Token (optional - if not set, authentication is disabled)
# Can be sent via X-Forward-Token header OR as ?token=xxx query parameter
# AUTH_TOKEN=your_secret_token_here

# Forward Path (optional - default: true)
# Set to false for fixed webhook URLs that don't accept additional paths
# When false, always forwards to TARGET_URL as-is and passes original URL via:
#   - X-Original-URL header
#   - original_url query parameter
# FORWARD_PATH=true
