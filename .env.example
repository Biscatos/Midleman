# Server Configuration
PORT=3000

# Target URL (where requests will be forwarded)
TARGET_URL=https://api.example.com

# Authentication Token (required in X-Forward-Token header)
AUTH_TOKEN=your_secret_token_here
